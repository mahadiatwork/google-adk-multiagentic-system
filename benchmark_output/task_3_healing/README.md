# CLI Expense Tracker

A simple command-line interface (CLI) tool to track expenses and save them to a CSV file.

## Installation

To install the CLI Expense Tracker, you need to have Python installed on your system. You can install the required dependencies using pip:

```bash
pip install -r requirements.txt